import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {},
  labelContainer: {
    marginBottom: 8,
  },
  chipsContainer: {
    flexDirection: "row",
    flexWrap: "wrap",
    rowGap: 8,
    columnGap: 6,
  },
  helperContainer: {
    marginTop: 4,
  },
  helperText: {
    opacity: 0.7,
  },
});
