import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  separator: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    width: "100%",
  },
  line: {
    height: 1,
  },
  text: {
    marginHorizontal: 16,
    minWidth: 30,
    textAlign: "center",
  },
});
