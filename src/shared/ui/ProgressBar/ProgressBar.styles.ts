import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    borderRadius: 4,
    overflow: "hidden",
    position: "relative",
    zIndex: 1,
  },
  progress: {
    height: "100%",
    borderRadius: 4,
  },
});
